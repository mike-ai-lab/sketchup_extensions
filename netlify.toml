[build]
  command = "pnpm run build:netlify"
  publish = "dist/public"

[build.environment]
  # Public client env vars are expected in the bundle; omit them from secrets scan.
  SECRETS_SCAN_OMIT_KEYS = "VITE_FRONTEND_FORGE_API_URL"
